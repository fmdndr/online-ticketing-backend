apiVersion: v1
kind: Service
metadata:
  name: basket-api
  labels:
    app.kubernetes.io/name: basket-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ticketing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: basket-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

