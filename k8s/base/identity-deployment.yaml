apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-api
  labels:
    app.kubernetes.io/name: identity-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ticketing
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: identity-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: identity-api
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: ticketing
    spec:
      imagePullSecrets:
        - name: dockerhub-pull-secret
      containers:
        - name: identity-api
          image: docker.io/fmdx/ticketing-identity:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: ticketing-config
            - secretRef:
                name: ticketing-secrets
          env:
            - name: Jwt__PublicKeyPath
              value: "/app/keys/rsa-public.pem"
            - name: Jwt__PrivateKeyPath
              value: "/app/keys/rsa-private.pem"
          volumeMounts:
            - name: rsa-keys
              mountPath: /app/keys
              readOnly: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          startupProbe:
            httpGet:
              path: /swagger/identity/index.html
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 18
          readinessProbe:
            httpGet:
              path: /swagger/identity/index.html
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /swagger/identity/index.html
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
      terminationGracePeriodSeconds: 30
      volumes:
        - name: rsa-keys
          secret:
            secretName: ticketing-rsa-keys
